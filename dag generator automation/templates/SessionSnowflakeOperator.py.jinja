

{{ task_id }} = SnowFlakeOperator(
    task_id='{{ task_id }}',
    sql="{{ sql_file }} ",
    conn_id="{{ conn_id }}",
    dag=dag,
    on_failure_callback=None,
    parameters={{ parameters }}
)

